<?xml version="1.0" encoding="utf-8" ?>
<resources>
  <!-- Inherit from the light Material Theme -->
  <style name="BlueTheme" parent="android:Theme.Material.Light">
    <!-- Override the app bar color -->
    <item name="android:colorPrimary">@color/my_blue</item>

    <!-- Override the color of UI controls -->
    <item name="android:colorAccent">@color/my_blue</item>

    <item name="android:windowBackground">@color/my_blue_bg</item>
  </style>

  <style name="YellowTheme" parent="android:Theme.Material.Light">
    <!-- Override the app bar color -->
    <item name="android:colorPrimary">@color/my_yellow</item>

    <!-- Override the color of UI controls -->
    <item name="android:colorAccent">@color/my_yellow</item>

    <item name="android:windowBackground">@color/my_yellow_bg</item>
  </style>

  <style name="GreenTheme" parent="android:Theme.Material.Light">
    <!-- Override the app bar color -->
    <item name="android:colorPrimary">@color/my_green</item>

    <!-- Override the color of UI controls -->
    <item name="android:colorAccent">@color/my_green</item>
    
    <item name="android:windowBackground">@color/my_green_bg</item>
  </style>

  <style name="PurpleTheme" parent="android:Theme.Material.Light">
    <!-- Override the app bar color -->
    <item name="android:colorPrimary">@color/my_purple</item>

    <!-- Override the color of UI controls -->
    <item name="android:colorAccent">@color/my_purple</item>

    <item name="android:windowBackground">@color/my_purple_bg</item>
  </style>
</resources>